import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
  },
  viewForScroll: {
    position: 'absolute',
    bottom: 20
  },
  navigationContainer: {
    width: '100%',
    height: '100%'
  },
  normalMapView: {
    width: '100%',
    height: '100%'
  }
});
export default styles